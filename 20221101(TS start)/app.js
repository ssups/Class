// typeScript
// JS에서 타입 설정이 추가된 언어 , 타입이 확장된 언어
// c++ c# 같은 객체지향 프로그래밍

//  JS는 코드를 실행해야 에러 확인 가능
//  TS는 작성한 코드가 문제가 있으면 실행시키기전에 문제 해결 가능

// TS 컴파일은 TS코드를 JS 코드로 바꿔줌
// JS로 바꾸는 이유는 브라우저는 JS 코드만 이해하기 때문

// npm i -g typescript

// tsconfig.json 생성명령어
// tsc --init

// 변수명 : 타입 = 초기값

// ts-node라는 TS 실행기를 사용해서 개발 환경에서 TS로 작성된 파일을 실행 시켜 볼 수 있다.

// npm i -D typescript ts-node @types/node
